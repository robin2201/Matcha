extends layout

block content
    .header
      h1.title: a(href='/') Matcha
      p Find Love around you!


    .jumbotron
        .col-sm-12
            .row
                if message
                    div.alert-danger.message #{message}
            .row

                .col-sm-4
                .col-sm-2
                    .registerForm
                        a(href='/register') Register 🍺
                .col-sm-2
                    .loginForm
                        a(href='/login') Login 🥂
            block register

            block login
